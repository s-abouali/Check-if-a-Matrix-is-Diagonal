# Check if a Matrix is Diagonal

This program reads a square matrix of size N × N, displays it, and checks whether it is a diagonal matrix.
A matrix is diagonal if all elements outside the main diagonal are equal to 0.
